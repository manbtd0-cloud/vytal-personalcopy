# Copy this file to .env and fill in your own keys.
# Never commit .env — it's already in .gitignore.

# Groq (LLaMA-3.3-70B) — used for AI explanation generation
VITE_GROQ_API_KEY=

# Qwen-Plus via Alibaba DashScope — secondary/fallback AI provider
VITE_DASHSCOPE_API_KEY=
