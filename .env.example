# Browser-safe values only. Row-level security must be enabled before use.
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=sb_publishable_replace_me

# Never add database secret/service-role, AI provider, webhook, or payment keys here.
# Server-only names are documented in supabase/functions/.env.example.

